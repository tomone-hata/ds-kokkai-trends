# 更新履歴

> **何をどう変えてきたか**の記録。現在地は `PROJECT_STATUS.md`、文書の一覧は `INDEX.md` を見ること。
> `docs/` 配下と同じく、**生の発言と話者を書かない**（`CLAUDE.md` 3章）。

## 記載ルール

### 1. 同じ日・同じ対象の変更は、最終状態のみを1行で書く

途中経過を残さない。AをBに変え、さらにBをCに変えた場合、**「AをCにした」と1行で書く**。
繰り返しの試行錯誤を全て残すと、後から読んだときに何が現在の状態か分からなくなるため。

```
NG:  | 収集処理 | 取得単位を会議単位に変更 |
     | 収集処理 | 取得単位を発言単位に変更 |
OK:  | 収集処理 | 取得単位を会議単位から発言単位に変更 |
```

### 2. 対象が異なれば、同じ日でも行を分ける

「新しい条件でクラスタリングを実験した」と「API取得の仕様を変えた」は別の対象であり、
まとめると意味が失われる。**正規化するのは対象が同じ場合に限る。**

「対象」は処理・機能・文書の単位で決める（例: 収集処理、クラスタリング、ドキュメント構成、データ層）。

### 3. 過去の日付の記述は書き換えない

正規化してよいのは**当日の作業中のみ**。前日以前の行は、誤記の修正を除いて変更しない。

### 4. gitの履歴と役割を分ける

コミット単位の機械的な記録はgitに任せる。ここには**判断と結果**を書く。
「なぜ」を詳しく書く必要がある場合はADRを起こし、`関連` 列からリンクする。

---

## 2026-08-18

| 対象 | 変更内容 | 関連 |
|---|---|---|
| リポジトリ | git初期化（`main`）し、GitHub（public / MIT License）へ公開。`.gitignore` に `outputs/` `private/` `.DS_Store` を追加 | - |
| プロジェクト規約 | `CLAUDE.md` に目的・フェーズ・データソース制約・外部LLM利用の規約を記載 | - |
| データ層 | PoCはParquet + DuckDBで完結させ、RDB・Dockerの導入は本実装フェーズからと決定 | [ADR-001](decisions/ADR-001-poc-data-layer.md) |
| ドキュメント構成 | `docs/` をID駆動（EXP / ISSUE / ADR）に再編。`INDEX.md`・`issues/`・`decisions/`・`drafts/`・`_templates/`・**`inputs/`** を新設し、運用規約を `.claude/rules/documentation.md` に定義。**`docs/inputs/` は追記のみ**とした（記録を事後に編集できると `[事実]` タグの検証が循環するため） | `.claude/rules/documentation.md` 3.6節 |
| 機密データの境界 | 取得データを機密扱いとし、境界を「生の発言と話者を出さない」に設定。`outputs/`・`private/` を新設 | [ADR-002](decisions/ADR-002-public-private-boundary.md) |
| 対象データ | PoCの対象を2025年（発言118,566件）、本番想定を2019〜2025年（同819,428件）と確定。2019年でコロナ前の基準年を確保し、年央で不完全な2026年を除外 | [ADR-001](decisions/ADR-001-poc-data-layer.md) 9章 |
| 実行環境 | uvでプロジェクトを初期化し、Pythonを3.12に固定。依存は httpx / duckdb / pyarrow / pydantic / tenacity、開発依存は pytest | - |
| ブランチ運用 | 作業をブランチで分け、**Pull Request 経由でマージ**する方式を採用（ローカルマージ禁止・Merge commit固定）。規約変更もブランチ経由とし、載せるブランチは影響範囲で判断する。実験も成果物として同一リポジトリに残すと定義 | [ADR-003](decisions/ADR-003-branch-strategy.md) / [ADR-004](decisions/ADR-004-rule-change-via-branch.md) / [ADR-005](decisions/ADR-005-merge-via-pull-request.md) |
| 要件定義 | 分析要件定義書のドラフトをv0.7まで作成。依頼者への確認事項を全件クローズし、LLMコスト見積もり・参照論点リストを要件化。deliverable-reviewerのレビュー1回目は差し戻し（致命的4・重要15・軽微8） | [1回目レビュー](reviews/レビュー_分析要件定義書-論点抽出PoC_20260818.md) |

## 2026-08-19

| 対象 | 変更内容 | 関連 |
|---|---|---|
| 要件定義 | レビュー3回（差し戻し2回→条件付き承認）を経て**v1.0として確定**。その後 [ISSUE-001](issues/ISSUE-001-requirements-analysis-unit-and-review-conditions/issue.md) で**分析単位をPoCの比較項目へ変更**しv2.0、[ISSUE-002](issues/ISSUE-002-requirements-ar006-and-unit-reference-scope/issue.md) で**AR-006受入条件の明確化と一致率の観測範囲の限定**を行い**v2.1**。未確定事項10件はPoC設計書で解消 | [3回目レビュー](reviews/レビュー_分析要件定義書-論点抽出PoC_v0.8_20260819.md)、[ISSUE-001](issues/ISSUE-001-requirements-analysis-unit-and-review-conditions/issue.md)、[ISSUE-002](issues/ISSUE-002-requirements-ar006-and-unit-reference-scope/issue.md) |
| PoC設計 | PoC設計書を **v1.0 として確定**し `design/` へ移動。着手前必須5件（Q-013・015・016・017・018）を確定し、実験内で決定する5件の比較設計を定義。**LLMコストの律速がTPMであり費用制限ではないことを確定**し、ベクトル化方式の決定要因を「品質」と「機密境界」へ移した。収集をマニフェスト方式で設計し期間を実行時の引数化。**レビュー3回（差し戻し2回→条件付き承認）**を経て、ステージの循環参照・Q-018の未統合・改訂の適用漏れを解消 | [PoC設計書](design/poc-design-topic-extraction.md)、[3回目レビュー](reviews/レビュー_PoC設計書-論点抽出_v0.11_20260819.md) |
| データ層 | **生データを可逆に保存する要件を追加**（`raw_json` 列）。取得と変換のコストが非対称であり、列マップ漏れによる再取得を避けるため | [ADR-001](decisions/ADR-001-poc-data-layer.md) 6.1節 |
| 上流の入力 | **国会会議録検索APIの仕様要約**と**参照論点リストの情報源**を `docs/inputs/` へ記録。参照A（施政方針演説）・参照B（提出議案）の**実在と見出し構造を確認**（レビューで3回未確認だった項目）。官邸サイトの掲載範囲の制約と、参照A・Bの粒度が1桁異なることが判明 | [API仕様の要約](inputs/kokkai-api-spec-summary.md)、[参照論点リストの情報源](inputs/reference-topic-sources.md) |
| 機密境界 | 参照論点リストの出典を、国会会議録APIとは独立した公開ソースへ変更。ADR-002の境界を解釈せずに済む形とした。**ただし解消するのは機密境界のみで、参照Aの内容がコーパス内に存在する点は残る**（ADR-006 訂正節） | [ADR-006](decisions/ADR-006-reference-topic-list-storage.md) |
| ドキュメント運用 | **マージ後のブランチ削除を規約化**（削除は承認を得てから実行）。**ADRについて決定を維持したまま根拠のみを訂正する手続き**を追加。`docs/inputs/` を公開・非公開の境界表へ明記 | `.claude/rules/documentation.md` 1・3.5・7節 |

## 2026-08-20

| 対象 | 変更内容 | 関連 |
|---|---|---|
| PoC設計 | **v1.0 から v1.5 へ改訂。** (1) 構成の確定ステージを S-7 に明示し、9.4節 規則2 の主観評価を SE-B に特定、2.4節の D-3 を「撤退判定には用いない／選定のための判定には用いてよい」と書き分け。(2) **H-009 を「時系列集計が動作し、会期・委員会構成という既知の構造を再現できる」へ改定**し、11章に交絡（比率化では解決しない）を明記、TM-4・TM-5 を追加。年内の時系列を**動作確認の対照実験**と位置づけた。(3) **`experiment-analyst` によるレビューポイント RP-A〜RP-D を追加**（2.4節・12.1節・12.3節）。CP は「無駄に走り続けること」しか止めず、**不可逆な決定を含む S-4・S-5・S-7 に判断の妥当性を問う機会が無かった**ため。RP-B を S-6（最重ステージ）の手前に置き手戻りを最大4.0人日に限定。(4) **6.1節に工程2A（会議録冒頭情報の除外）を追加**。`speechOrder = 0` の1,126件は発言ではなく会議録の front matter であり、短文除外では落ちず**最大値46,976字の正体**であった（除くと16,320字）。生データからは削除せず議題情報として Phase 2 で再利用しうる旨を明記 (5) 17章の依存追加手順を `uv add` から `pyproject.toml` の直接編集へ改め、**比較候補は既定で `exp` グループに置き採用確定時に `dependencies` へ移す**旨を追記 | [ISSUE-003](issues/ISSUE-003-poc-design-stage-and-decision-rules/issue.md) / [ISSUE-005](issues/ISSUE-005-timeseries-scope-and-committee-confound/issue.md) / [ADR-008](decisions/ADR-008-review-checkpoints-in-large-experiments.md) / [ISSUE-008](issues/ISSUE-008-meeting-record-header-exclusion/issue.md) / [ADR-009](decisions/ADR-009-dependency-declaration.md) |
| 実験計画 | EXP-001 の `plan.md` を作成。**撤退基準を数値で確定**（D-1a: 累計10,000件時点で2025年への外挿が24時間超なら中断、D-1b: 稼働24時間超、D-1c: 本番外挿168時間超で撤退、ほか D-2・D-3）。実施後に書き換えない文書として確定 | - |
| 収集処理 | S-1 の収集スクリプトを実装し、**2025年の全件 118,566件（期待値と完全一致・`speechID` 重複0）を収集完了。** 月単位でマニフェストに進捗を残し、中断地点から再開できる設計とした。**接続断のリトライを2段構成に変更**（19001 の内側リトライとは別に、トランスポート層を30〜600秒の指数バックオフで5回）。**0件の月は Parquet を作成しない**よう修正（`from_pylist([])` が0行0列となり、グロブ読み込みが壊れるため） | [ISSUE-004](issues/ISSUE-004-collector-transport-error-retry/issue.md) / [ISSUE-006](issues/ISSUE-006-empty-partition-schemaless-parquet/issue.md) |
| データ収集（S-1） | **CP-1 を通過。** 稼働 0.30 時間・実測 398,378件/時。本番規模 819,428件への外挿は **2.06 時間**（D-1b・D-1c ともに抵触せず）。ディスク使用量 91 MB。あわせて実測値を確定: 会議数1,126、総文字数49,299,337、文字数の中央値318（**1月のみの標本 196 とは大きく異なり、1月は代表性を欠いていた**）、20文字以下は2.96%。**7月は0件**（閉会期間）、**9月は単一委員会で247件** | [ISSUE-005](issues/ISSUE-005-timeseries-scope-and-committee-confound/issue.md) |
| 実験の運用規約 | **多段の実験では、不可逆な決定を含むステージの直後に `experiment-analyst` の検証を挟むと決定。** 従来の「実験の終了時に依頼する」は1回で完結する小さな実験を前提としており、段階的な貪欲決定では**誤った前提が矛盾として表面化しないまま下流へ伝播する**（撤退基準は「無駄に走り続けること」しか止めない）。レビューポイントの置き方5原則を `.claude/rules/experiment-code.md` に定義。**全ステージには置かない**（追認の儀式化を避けるため） | [ADR-008](decisions/ADR-008-review-checkpoints-in-large-experiments.md) |
| データ品質確認（S-2） | **RP-A を3回実施し（差し戻し2回 → 通過）、S-2 を完了。** 初回は設計書 6.1節・6.2節・6.3.1節を確認せず実装しており、**測定対象（生 vs 正規化後）・ビン境界・ビン解像度・重複率の定義・正規化の順序（3-1 NFKC 未実装）・T2 の導出規則**の6点が設計と相違していた。話者表記が平均15.53字付くため「5文字以下0件」「短文は大量に存在しない」は**測り方の産物**であり、**≤10字は 12件 → 3,454件（2.941%）と反転**。是正後、6.3.1節 手順3・4 により **T1 = 10 / T2 = 20 が機械的に確定**。**6.2節 3-3 の除去パターンを NFKC 適用後の表記で確定し `config.md` に記録**（設計書が S-2 の成果物として要求していたが未実施だった）。過剰除去・過小除去とも0件を確認 | [ISSUE-007](issues/ISSUE-007-s2-aggregation-deviates-from-design/issue.md) / [RP-A](reviews/RP-A_EXP-001-S2-data-quality_20260820.md) |
| 依存管理の規約 | **`uv add` / `pip install` を禁止し、`pyproject.toml` の直接編集 + `uv lock` → `uv sync --frozen` に変更。直接依存は `==` で完全固定する。** `uv add` は実行時点の最新版を `>=` で書き込むため**宣言と実体が乖離**し、暗黙にロックも更新するため。**形態素解析辞書は版が変われば分かち書き結果が変わり、乱数シードでは担保できない**（S-3 で SudachiPy を使う）。あわせて**実験専用の依存を `[dependency-groups] exp` へ分離**し、本番実装での採用が確定した時点で `dependencies` へ移すことにした（落選した候補を本番依存に残さないため。`src/` から `experiments/` を import しない原則と同じ）。既存5パッケージも `==` へ揃えた（**版の変化なし**）。**S-3 の依存（`sudachipy` 0.6.11・`sudachidict-core` 20260723・`scikit-learn` 1.9.0・`numpy` 2.5.2・`scipy` 1.18.0）を `exp` へ追加**し、本番同期（`uv sync --frozen`）では導入されないことを実測で確認 | [ADR-009](decisions/ADR-009-dependency-declaration.md) |
| 規約の適用範囲 | **規約変更を過去へ遡って適用しないと決定。** 旧規約下の文書・コードを「誤り」として扱わず、**旧規約のままで実害（結果の再現性・機密境界・安全性・規約間の衝突）が出る箇所に限り個別に見直す**。**エージェントによる開発では遡及的な修正を抑制するものが無く、規約が変わるたびに全体を書き換える方向へ働くため**（修正のたびに壊す機会が入り込む）。**立証責任は改訂する側**が負い、「望ましい」「様式が揃っていない」は根拠にならないと明記した。据え置いた判断は当該規約のADRへ一覧で記録する | [ADR-010](decisions/ADR-010-no-retroactive-revision.md) |
| 分かち書きの決定（S-3） | **W-2（SudachiPy 分割モードA）を採用。** 4方式を全件117,440件で比較し（E-0・U-1・k=100 の暫定固定）、決定規則1（SE-A 解釈可能率）で単独首位（**W-2 35% > W-1 25% > W-3 20% > W-4 10%**）。同率がないため WM-3 は算出せず、2024年議案件名リストの作成は不要となった。**撤退基準 D-2 は全項目通過**（最大でベクトル化9.6秒・クラスタリング10.7分・メモリ11.93GB）。**文字n-gramは形態素解析に対し語彙4〜18倍・クラスタリング時間14〜29倍**。設計書に定めのない `min_df`=5 / `max_df`=0.5 / `n_init`=3 を担当判断で設定し記録 | [config.md](../experiments/EXP-001-topic-extraction-baseline/config.md) |
| 仮説 H-010 の初回観測 | **議事進行クラスタは4方式すべてで独立に分離された**（全体比 1.40〜2.91%）。前処理で除外しなくてもクラスタ単位で「論点ではない」と識別できる。ただし**方式間で2倍の幅があり**、「方式によらず同程度に存在する」という前提は厳密には成り立たない。**W-2 の評点0は10件すべてが議事進行以外**であり、議事進行クラスタが解釈可能率を押し下げた事実はない | 設計書 6.3A.6節 |
| 評価者の比較（記録） | **LLM 2種による参考評価を実施**（決定には未使用）。**LLM同士は91%一致するが人とは40%**で、80件中25件が0対2の正反対。LLMの解釈可能率は全方式55〜60%と一貫して甘く、**首位も異なる**（LLM→W-1、依頼者→W-2）。**LLMを評価者にすると別の方式を採用していた**。初回プロンプトに担当が示した評点付きの例が混入しており汚染の疑いがあるが、決定に用いていないため取り直しは行わなかった | `CLAUDE.md` 4章 |
| 前処理水準の決定（S-4） | **E-2（文字数 ≤20 を除外・6,014件 5.12%）を採用。** 決定規則1（SE-A 解釈可能率）で単独首位（**E-2 50% > E-0 35% = E-1 35%**）。**E-1（T1=10）は E-0 と同率で、T1 水準の除外はほぼ効果がなかった。** E-1 と E-2 の差はわずか2,560件（2.18%）だが15ポイントの改善を生む。**S-2 で T1 を機械的に決めた手順（6.3.1節 手順3）は解釈可能率の観点では最適でなかった**ことになるが、事後の基準変更にあたるため今フェーズでは手順を変更せず Phase 2 への申し送りとする。E-0 は S-3 の W-2 と条件が完全一致するため再計算せず再利用した（評価は40件で実施） | [config.md](../experiments/EXP-001-topic-extraction-baseline/config.md) |
| 計算指標と主観評価の乖離（記録） | **最大クラスタ占有率だけを見れば E-1 が最良**（6.96% 対 E-2 8.42%）**だが、解釈可能率は E-2 が明確に高い**（50% 対 35%）。決定規則1 は解釈可能率のみで決めるため選定には影響しないが、**計算指標が主観評価の代理にならない**ことを示す実測として記録した。あわせて**短文除外は語彙をほとんど削らない**ことも判明（20,674 → 20,665、差9語）。除外される短文の語は他の発言にも現れるため、文書の分布だけが変わる | 設計書 6.3.2節 |
