# EXP-001 実行条件の記録

> **これが無い実験結果は本設計フェーズで再現できず、破棄される**（`.claude/rules/experiment-code.md` 禁止事項2）。
> ステージごとに追記する。**過去の記録を書き換えない。**

## 実行環境（2026-08-20 実測）

| 項目 | 値 |
|---|---|
| OS | macOS 26.3.2（arm64） |
| CPU | arm（Apple Silicon） |
| **物理メモリ** | **48.0 GB** |
| Python | 3.12.13（`.python-version` で固定） |
| MPS | **未確認**（`torch` 未導入。S-6 の V-3 実施時に確認する） |

**D-2d（物理メモリ使用率70%）の絶対値は 33.6 GB** である（48.0 GB × 0.70）。

### ライブラリのバージョン

| パッケージ | 版 |
|---|---|
| httpx | 0.28.1 |
| duckdb | 1.5.5 |
| pyarrow | 25.0.1 |
| pydantic | 2.13.4 |
| tenacity | 9.1.4 |

## 乱数シード

**42**（Python標準・NumPy・使用フレームワークすべて）。

**S-1 には乱数を用いる処理がない。** 後段のステージ（サンプリング・クラスタリング）で固定する。

---

## S-1: データ収集

| 項目 | 値 |
|---|---|
| スクリプト | `run_01_collect.py` |
| コミットハッシュ | **`06270ba`**（`run_01_collect.py` と `plan.md` を確定させたコミット。全件収集はこの版で実行する） |
| 実行日時 | 2026-08-20 |

### 入力データ

| 項目 | 値 |
|---|---|
| 取得元 | `https://kokkai.ndl.go.jp/api/speech` |
| **仕様の確認日** | **2026-08-20**（原典 `https://kokkai.ndl.go.jp/api.html` を実行により再確認） |
| 抽出条件 | `from` / `until` を月単位で指定。他の検索条件を付けない（全件取得） |
| 出力先 | `data/raw/year=YYYY/month=MM/speech.parquet`（Git管理対象外） |
| マニフェスト | `data/raw/collect_manifest.json` |

### コード中に直接書き込んだ値

**ハードコードは許容するが、記録しないことは許容しない**（規約 禁止事項2）。

| 定数 | 値 | 根拠 |
|---|---|---|
| `MAXIMUM_RECORDS` | **100** | 仕様上の上限（`speech` は1〜100、既定30） |
| `WAIT_SECONDS` | **3.0** | `CLAUDE.md` 0章「1リクエストごとに数秒空ける」。**リクエスト前**に待つ |
| `TIMEOUT_SECONDS` | 60.0 | — |
| `RETRY_ATTEMPTS` | 5 | 19001（混雑）のみ。指数バックオフ（min 4秒 / max 120秒） |
| `PROGRESS_EVERY` | 20 | 進捗ログの間隔（リクエスト数） |
| `D1A_THRESHOLD_RECORDS` | 10,000 | plan.md 3.1節 D-1a の判定時点 |
| `D1A_PROJECTED_HOURS` | 24.0 | 同 D-1a・D-1b の閾値 |
| `D1C_PROJECTED_HOURS` | 168.0 | 同 D-1c の閾値（7日） |
| `POC_TOTAL_EXPECTED` | 118,566 | 2025年の想定件数（API実測値） |
| `PROD_TOTAL_EXPECTED` | 819,428 | 2019〜2025年の想定件数（同上） |
| 圧縮 | zstd | Parquet の書き込み |
| `TRANSPORT_RETRY_ATTEMPTS` | **5** | [ISSUE-004](../../docs/issues/ISSUE-004-collector-transport-error-retry/issue.md)。接続断のリトライ回数 |
| `TRANSPORT_WAIT_MIN` | **30.0** | 同上。切断の原因が未特定（keep-alive競合 / アクセス遮断）のため長めに取る |
| `TRANSPORT_WAIT_MAX` | **600.0** | 同上 |

### 2025年1月の収集（動作確認を兼ねる）

**1月分は `aac47b8`（未コミット状態）のコードで取得した。** その後スクリプトに変更を加えていないため、`06270ba` の版と同一である。マニフェストで完了済みとなっており、全件収集では再取得されない。

| 項目 | 実測値 |
|---|---|
| 期待件数（`numberOfRecords`） | 1,001件 |
| 取得件数 | **1,001件（一致）** |
| `speechID` の重複 | **0件** |
| リクエスト数 | 11 |
| 稼働時間 | 0.01 時間（約35秒） |
| 実測スループット | 約 102,775 件/時 |
| 19001（混雑）の発生 | **0回** |

**CP-1 の暫定判定**: 本番規模 819,428件への外挿 **7.97 時間（0.3日）**。D-1b・D-1c ともに抵触しない。

### 実測により判明した事実

**plan.md 8節「想定される結果」の予想4は成立した。**

| # | 事実 |
|---|---|
| 1 | `numberOfRecords` は当該検索条件の総件数を返す。**取りに行く前に期待値が確定できる** |
| 2 | `nextRecordPosition` によるページングで取りこぼしなく全件取得できた（欠番・重複なし） |
| 3 | `maximumRecords=100`（上限）が安定して使えた |
| 4 | レスポンスの1レコードは **21キー**。Parquet は `raw_json` とパーティション列（year・month）を加えて **24列** |
| 5 | **`issueID` 列が存在する**（S-5 の U-2 構築で会議単位のグループキーに使う。18章 U-14） |
| 6 | **`speechOrder` 列が存在する**（発言の出現順。U-2 の連結順に使える。同上） |

### 発言の文字数分布（2025年1月・1,001件）

| 統計量 | 値 |
|---|---|
| 平均 | 629.4 文字 |
| 中央値 | 196.0 文字 |
| 90パーセンタイル | 1,042.0 文字 |
| 最大 | 11,389 文字 |
| 20文字以下 | 22件（2.2%） |

**平均が中央値の3.2倍であり、右に長い裾を持つ。** plan.md 8節の予想3と整合する。

**ただしこれは1か月・1,001件の値である。** 2025年全体（118,566件）の分布は S-2 で確定させる。1月は通常国会の召集前後で会議が少なく、代表性に欠ける可能性がある。

### 全件収集の1回目（2026-08-20・中断）

**約46分・869リクエスト・86,741件の時点で接続断により停止した**（[ISSUE-004](../../docs/issues/ISSUE-004-collector-transport-error-retry/issue.md)）。

| 項目 | 実測値 |
|---|---|
| 実行時間 | 10:19:37 〜 11:05:35（約46分） |
| 完了した単位 | 2025年1月〜5月（**85,141件**） |
| 中断した単位 | 2025年6月（`startRecord=2401`） |
| リクエスト数 | 869 |
| 19001（混雑）の発生 | **0回** |
| 接続断 | **1回**（`RemoteProtocolError`） |

**`D-1a` は通過した。** 実測 **125,277件/時**、2025年118,566件への外挿 **0.95時間**（閾値24時間）。

**この時点で得られた月別の期待件数**

| 月 | 期待件数 | 取得件数 |
|---|---|---|
| 2025-01 | 1,001 | 1,001 |
| 2025-02 | 13,165 | 13,165 |
| 2025-03 | 20,713 | 20,713 |
| 2025-04 | 26,344 | 26,344 |
| 2025-05 | 23,918 | 23,918 |
| 2025-06 | 11,887 | （中断） |

**1月が突出して少ない**（1,001件）。通常国会の召集が1月下旬であるため。**月による会議数の偏りが大きい**ことが確認できた。

### 全件収集の2回目（2026-08-20・完了）

[ISSUE-004](../../docs/issues/ISSUE-004-collector-transport-error-retry/issue.md) の修正後、6月から再開して完了した。**1回目で完了済みの1〜5月は再取得していない。**

#### 実行したコードの版（`.claude/rules/experiment-code.md` 禁止事項2）

**本文上部の S-1 表は「コミットハッシュ `06270ba`。全件収集はこの版で実行する」と記しているが、2回目の実行はこの版ではない。** 過去の記録は書き換えず、ここに実際の版を記す。

| 項目 | 内容 |
|---|---|
| 基準コミット | `06270ba` |
| **実行時の状態** | **未コミットの変更あり**（`run_01_collect.py` に +39 / −4 行） |
| 変更の内容 | [ISSUE-004](../../docs/issues/ISSUE-004-collector-transport-error-retry/issue.md) の対策（トランスポート層リトライの追加） |

**1〜5月（1回目）と6〜12月（2回目）は異なる版のコードで取得した。** 1回目は `06270ba` と同一内容、2回目は上記の未コミット版である。**リトライ処理の追加のみで、取得・保存のロジックは変更していない**ため、データの同質性には影響しない。

さらに収集完了後、[ISSUE-006](../../docs/issues/ISSUE-006-empty-partition-schemaless-parquet/issue.md) の対策（0件の月を書き出さない）を加えている。**この変更は収集結果に対して事後であり、2回目の実行時点では含まれていない。**

| 項目 | 実測値 |
|---|---|
| 実行時間 | 11:21:32 〜 11:39:20（**約18分**） |
| 収集した単位 | 2025年6月〜12月 |
| **累計取得件数** | **118,566件（期待値と完全一致）** |
| `speechID` の重複 | **0件** |
| 19001（混雑）の発生 | **0回** |
| 接続断 | **0回** |

#### CP-1 の判定（plan.md 3.1節）

| 基準 | 閾値 | 実測 | 判定 |
|---|---|---|---|
| D-1b | 稼働24時間超 | **0.30 時間** | **通過** |
| D-1c | 本番外挿168時間超 | **2.06 時間**（0.1日） | **通過** |

実測スループット **398,378 件/時**。1月単独の実測（102,775件/時）より高い。**`maximumRecords=100` が安定して使え、待機3秒が支配的であるため、1リクエストあたりの件数が多い月ほど効率が上がる。**

**本番規模 819,428件は約2.1時間で収集できる見込みである。** D-1（収集の成立性）は成立した。

#### 検証（ISSUE-006 で追加した4項目）

| # | 項目 | 結果 |
|---|---|---|
| 1 | `data/raw/` 全体をグロブで読み込めること | **成功**（ISSUE-006 の修正後） |
| 2 | 総件数が期待値と一致 | **118,566 = 118,566** |
| 3 | `speechID` の重複 | **0件** |
| 4 | 全パーティションで列構成が一致 | **一致**（全11ファイルが22列） |

ディスク使用量 **91 MB**（zstd圧縮、Parquet）。**本番7年分でも 700 MB 程度**であり、ADR-001 の「PoC段階でRDBを導入しない」判断を支持する。

#### 収集した2025年データの実測値

| 項目 | 値 |
|---|---|
| 発言数 | 118,566 |
| 会議数（`issueID` のユニーク） | **1,126** |
| 総文字数 | **49,299,337 文字** |
| 院の内訳 | 衆議院 67,140 / 参議院 51,266 / 両院 160 |

**院に「両院」が160件存在する。** 両院協議会。件数は少ないが値として現れるため、集計時に3値であることを前提とする。

##### 発言の文字数分布（2025年全体・118,566件）

| 統計量 | 値 | 1月のみ（1,001件） |
|---|---|---|
| 平均 | **415.8** | 629.4 |
| 中央値 | **318.0** | 196.0 |
| p25 | 141.0 | — |
| p75 | 527.0 | — |
| p90 | **806.0** | 1,042.0 |
| p99 | 2,220.4 | — |
| 最大 | **46,976** | 11,389 |

**1月の分布は2025年全体を代表していなかった。** 平均は3割低く、中央値は逆に6割高い。平均÷中央値は **1月 3.2 → 全体 1.31** であり、**裾の重さの見え方が大きく異なる。**

`config.md` 上部に記した「1月は代表性に欠ける可能性がある」という留保は妥当だった。**S-2 以降の閾値設定には全体の分布を用いる。**

##### 短文の割合（前処理の除外閾値の基礎。設計書 Q-003）

| 閾値 | 件数 | 割合 |
|---|---|---|
| 10文字以下 | 12 | 0.01% |
| **20文字以下** | **3,507** | **2.96%** |
| 50文字以下 | 11,165 | 9.42% |
| 100文字以下 | 22,374 | 18.87% |

**10文字以下は12件しかない。** 「異議なし」等の定型的な短文が大量に存在するという想定は成立しなかった。**除外閾値を20文字に置いても失うのは3%である。**

##### 月別の構造（[ISSUE-005](../../docs/issues/ISSUE-005-timeseries-scope-and-committee-confound/issue.md) の根拠データ）

| 月 | 発言数 | 会議数 | ユニーク会議名 | 予算委員会シェア |
|---|---|---|---|---|
| 1月 | 1,001 | 39 | 21 | 38.8% |
| 2月 | 13,165 | 72 | 20 | 36.7% |
| 3月 | 20,713 | 167 | 29 | 23.8% |
| 4月 | 26,344 | 195 | 38 | 1.9% |
| 5月 | 23,918 | 180 | 34 | 1.9% |
| 6月 | 11,887 | 146 | 38 | 1.4% |
| **7月** | **0** | — | — | — |
| 8月 | 972 | 69 | 36 | 40.9% |
| **9月** | **247** | **2** | **1** | **100.0%** |
| 10月 | 356 | 28 | 19 | 0.0% |
| 11月 | 11,341 | 103 | 32 | 13.3% |
| 12月 | 8,622 | 125 | 38 | 21.4% |

**予算委員会シェアは `nameOfMeeting` の完全一致で算出した。** 部分一致（`LIKE '%予算委員会%'`）では分科会・公聴会を含むため2月が70.6%となる。**両者は別物であり、混同すると倍近く変わる。**

**9月は2会議・1会議名（予算委員会のみ）で247件である。** この月の「論点」は会議の選択そのものであり、**月次の時系列上では1点が単一委員会に占有される。** ISSUE-005 の交絡は、月によっては交絡どころか完全な決定関係になる。

##### 2月に分科会が集中する

| 会議名 | 件数 |
|---|---|
| 予算委員会 | 4,828 |
| 予算委員会第一〜第八分科会 | 各 486〜564（計 4,217） |
| 予算委員会公聴会 | 243 |

**分科会は省庁別に議題が分かれている。** 会議名が異なるため別クラスタに分かれる可能性があり、**S-5（分析単位）と S-7（構成確定）で会議名の扱いを決める際の材料となる。**

---

## S-2 以降

（各ステージの実施時に追記する）
