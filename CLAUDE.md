# プロジェクトルール

> **本ファイルは全セッションで読み込まれる。150行以内を維持すること。**
> 領域ごとの詳細規約は `.claude/rules/` にパススコープ付きで配置し、ここには索引と全体で不変の事項のみを置く。
> 個別の設計内容・障害ログ・進捗は `docs/` へ。

## 0. プロジェクト概要

- **目的**: 国会会議録の発言を教師なし学習で「論点」に分解し、その盛衰を時系列で可視化する。頻出語の集計ではなく、事前にトピックを定義せずクラスタとして論点を抽出することを狙いとする
- **現在のフェーズ**: PoC（2025年の1年分で論点抽出の成立性を検証する。本番の対象期間は2019〜2025年を想定）

### データソースの制約

国会会議録検索API（`https://kokkai.ndl.go.jp/api.html`、認証・利用手続き不要）が唯一のデータソース。以下は領域を問わず適用する。

- **逐次アクセスのみ。** 多重リクエストを行わない。1リクエストごとに数秒空ける。並列化による収集の高速化は禁止（提供元がアクセス遮断措置を明示している）
- **収集は中断・再開可能に実装する。** 取得済みの範囲を永続化し、再実行時に同じ範囲を取り直さない
- **取得データをGit追跡対象へ書き込まない**（3章）。著作権は個々の発言者に帰属し、情報解析目的の利用は権利制限規定の範囲だが本文の再配布は範囲外。加えて本プロジェクトでは機密データとして扱う
- リトライしてよいのはエラーコード19001（混雑）のみ。検索条件不正（19004〜19020）はリトライせず即座に失敗させる

## 1. 規約の構成

規約は領域ごとに分割されている。**扱うファイルの場所によって適用される規約が変わる。**

| 規約ファイル | 適用範囲 | 内容 |
|---|---|---|
| 本ファイル | 全体 | 機密・シークレット・Git・実行環境・全体で不変の原則 |
| `.claude/rules/experiment-code.md` | `experiments/`, `notebooks/` | 実験コード規約（**ハードコード可**） |
| `.claude/rules/production-code.md` | `src/`, `scripts/` | 本番コード規約（**ハードコード禁止**） |
| `.claude/rules/testing.md` | `src/`, `tests/` | テストファースト手順とテスト観点 |
| `.claude/rules/documentation.md` | `docs/` | 文書の作成・改訂フロー、ID体系、課題・決定の管理 |

読み込み状況は `/memory`、コンテキスト内訳は `/context` で確認できる。**新規ファイルを作成する場合、そのパスの規約がまだ読み込まれていないことがある。** 新規作成に着手する前に該当ルールを明示的に読むこと。

## 2. 全体で不変の原則

領域を問わず常に適用される。ここに書かれた事項は、他の規約より優先する。

1. **設計書と整合しない実装を行わない。** 実装前に該当する要件定義書・設計書を確認し、齟齬があれば報告してから進める
2. **迷った場合は手を止め、ユーザーに判断を仰ぐ。** 不確かなまま実装を進めない
3. **`src/` の実装はテストファースト。** テストを書き、失敗を確認してから実装する。テストを実装に合わせて書き換えない
4. **実験コードを本番コードへコピーしない。** アルゴリズム設計書を経由して書き直す
5. **乱数シードを必ず固定する**（Python標準・NumPy・使用フレームワーク）
6. **規約が変更されても既存コードを自動修正しない。** 新規コードと明示的に依頼されたコードにのみ適用する。スコープ外の違反は報告にとどめる
7. **`docs/` は全て公開可能なものだけを置く。** 発言本文・生レスポンス・機密データを入れない。公開できないものは `private/` へ置き、`docs/` からはパスで参照する。**迷ったら `private/` に置く**

## 3. 機密データ

### Git管理対象外

`.gitignore` で除外する。コミット前にステージング内容を確認すること。

- `data/`（生データ・中間生成物）/ `outputs/`（実験の全出力）/ `private/`（機密データ・非公開の証跡）/ `models/` — うち `outputs/` `private/` は `README.md` のみ追跡
- `.env`, `secrets/` — 認証情報
- `.claude/` — セッションデータ（`rules/`, `commands/`, `agents/` のみ追跡）

### ドキュメント・コードへの記載禁止

設計書、レポート、コードコメント、コミットメッセージ、テストデータに、**個人を特定しうる値**（実ID、氏名、担当者名、メールアドレス、電話番号）、**自由記述テキストの実データ**、**実顧客名・実案件名**を直接記載しない。ユーザーが明示的に生データの使用を指示した場合のみ使用してよいが、識別子の記載とGit追跡対象への書き込みは引き続き禁止する。

部門名・拠点名などの組織区分は**集計軸として記載してよい**。ただし構成員が少数で個人が特定できる粒度になる場合は伏せる。

例示にはダミーデータを使い「※ ダミーデータです」と明記する。IDは `xxxxxxxx-xxxx-...` の形に伏せ、組織名・人名は「○○部」「A社」とする。カラム名・型・件数・統計値の記載は問題ない。

### 国会会議録APIから取得したデータの扱い

**取得データは機密データとして扱う。** 出典が公開情報であることを理由に緩めない（顧客アンケートの自由記述や社内機密データを扱う案件と手順を揃え、その検証を兼ねるため）。境界は「**生の発言と話者を出さない**」に置く。

| 区分 | 該当するもの | `docs/` への記載 |
|---|---|---|
| **生の発言と話者** | 発言本文、発言者名、`speechID` / `issueID` | **禁止**（引用も不可） |
| **集計軸** | 開催日、院名、会議名、国会回次、号数、所属会派、肩書き | 集計した形でのみ可 |
| **集計値** | 件数、統計値、クラスタ数・サイズ、時系列の推移、クラスタの特徴語 | 可 |

- **集計軸を個票と結びつけて書かない。** 単独では個人を指さない値でも、組み合わせると特定できる（例: 開催日＋肩書き）
- 個票が必要な場合は `data/` または `private/` に置き、`docs/` からは `speechID` を伏せた上でパスのみ参照する
- **クラスタの特徴語に個人名が現れた場合は伏せる**（「議員A」）

## 4. シークレット管理

- APIキーの取得は共通ローダに一元化する。個別スクリプトで `load_dotenv()` を直接呼ばない
- 環境変数が設定済みならそれを使い、未設定なら `.env` から読み、無ければ明示的に例外で失敗させる
- **ログ出力禁止**: キー値を `print` / `logging` / 例外メッセージに含めない
- **ハードコーディング禁止**: コード・設定ファイル・ノートブックに直接書かない
- 本番・コンテナ環境では `.env` を配置せず、OSまたはコンテナの環境変数で渡す
- `~/.claude/settings.json` に `{ "permissions": { "deny": ["Read(./.env)", "Read(./.env.*)"] } }` を設定する（リポジトリに含まれないため環境ごとに手動設定）

### 外部LLMの利用

クラスタの命名・解釈に外部LLM APIを使用する。**送信するデータは機密扱い**（3章）であり、以下を守る。

- **ステートレスな呼び出しのみ使用する。** ファイルアップロード、バッチ処理など、サーバー側に状態が残る機能を使わない。SaaS型のLLMトレーシングツールも使用しない
- **既定で送ってよいのは集計値まで**（クラスタの特徴語、件数、統計値）。**個票（発言本文）を送る場合はADRで判断を記録してから行う**
- **LLM出力は乱数シードで固定できない。** モデル名・プロンプト・温度・入出力を実験メタデータとして保存し、再現の代わりに追跡可能にする
- 命名結果を後段の集計にそのまま流し込まない。クラスタIDを主キーとし、名前は表示用の属性として扱う

## 5. Git運用

- **Git操作はユーザーから明示的な指示がある場合のみ実行する。** 対象は `commit` / `push` / `merge` / `rebase` / `reset` / `cherry-pick` / PR作成 / ブランチ切り替え・強制更新など、リポジトリ状態を変更するすべての操作。「作業完了」と判断しても自動的にコミットしない
- 読み取り専用操作（`log` / `diff` / `status` / `show`）は自由に実行してよい
- コミット前に、生データ・認証情報・モデルファイルがステージングされていないか確認する。フェーズ完了時に annotated tag を打つ

### ブランチ運用（ADR-003）

**作業はブランチを切って行う。`main` へ直接コミットしない。**
**マージは必ず Pull Request 経由で行う。ローカルでの `git merge` による `main` への統合を禁止する**（ADR-005）。マージ方法は Merge commit に固定し、Squash / Rebase を使わない。長命ブランチが `main` を取り込む方向（`git merge main`）はローカルで行ってよい。

| 種別 | ブランチ名 | マージの条件 |
|---|---|---|
| 実験 | `exp/EXP-NNN-{slug}` | `plan.md` と `report.md` が揃っている |
| 課題・不具合 | `issue/ISSUE-NNN-{slug}` | `issue.md` が「対応済」または「仕様として受容」 |
| 決定・設計改訂 | `adr/ADR-NNN-{slug}` | 影響する文書の改訂履歴が更新されている |
| 実装 | `feat/{slug}` | テストが通る |
| 新規文書の作成 | `docs/{slug}` | `INDEX.md` に登録され、`drafts/` から確定先へ移動している |
| フェーズ単位の作業 | `poc/{slug}` など | 当該フェーズの成果物が揃い、レビュー判定が出ている |

**結果がNo-Goの実験もマージする**（失敗した実験ほど記録価値が高い）。**実験も成果物**であり本番コードと同一リポジトリに残すが、`src/` から `experiments/` を import してはならない。**規約自体の変更（`CLAUDE.md`・`.claude/rules/`・ADR）もブランチ経由とする。例外は設けない**（ADR-004）。どのブランチに載せるかは影響範囲で判断し、**その作業に閉じるなら当該ブランチに含め、他の作業にも及ぶなら規約配信用のブランチを切る。** 規約の効力は決定時点から生じ、ファイルの所在とは独立である。

## 6. 実行環境

- 実行: `uv run python {script}`（`python3` / `.venv/bin/python` は使わない）
- 依存追加: `uv add {package}`（`pip install` は使わない）
- テスト: `uv run pytest tests/`
- 実行の起点は常にリポジトリルート。スクリプト内で相対パスの起点を仮定しない

### データ層（PoC）

PoCではRDBとDockerを導入せず、ファイルベースで完結させる。本実装フェーズでのPostgres移行を機械的作業に留めるため、以下を守る。

- 生データは**Parquet形式**で保存し、**APIのフィールド名をそのまま列名にする**（`COPY` でそのまま投入できる形を保つ）
- **`speechID` を主キーとして扱う。** 冪等な再収集の判定もこれで行う
- `data/raw/year=YYYY/month=MM/` の形で**日付パーティション**に配置する
- 集計・分析のクエリは**DuckDBのSQLとして書く**。pandasでの手続き的な集計を既定にしない（移行時の書き直しを減らすため）

## 7. ドキュメント

**運用フローの詳細は `.claude/rules/documentation.md`。** ここには入口のみ置く。

| 入口 | 場所 |
|---|---|
| **いま何をしているか**（フェーズ・ブロッカー・直近の決定） | `docs/PROJECT_STATUS.md` |
| **何が存在するか**（全文書の目録・版数・状態） | `docs/INDEX.md` |
| **何をどう変えてきたか**（更新履歴） | `docs/CHANGELOG.md` |

**更新履歴は同じ日・同じ対象なら最終状態のみを1行で書く**（対象が異なれば同日でも分ける）。
ID体系は `EXP-NNN`（実験）/ `ISSUE-NNN`（課題・不具合）/ `ADR-NNN`（決定記録）。
確定した設計書は `docs/design/`、検討中の新規文書は `docs/drafts/`。**実装が参照してよいのは `docs/design/` と `docs/requirements/` の「現役」文書のみ。**
確定文書の改訂は作業ブランチ上で行い、`ISSUE-NNN` または `ADR-NNN` の起票を先行させる。

**個別の設計内容・障害番号・進捗状況を本ファイルに書かない。** 上記の入口を経由すること。
